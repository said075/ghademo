name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome_message
        run: echo "My first GHA job"

      - name: List files
        run: ls

      - name: Reaad file
        run: cat README.md
