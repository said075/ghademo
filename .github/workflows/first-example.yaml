name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome_message
        run: echo "My first GHA job"

      - name: Run a script
        run: |
          echo "This is a multi-line script"
          echo "It runs in the same step"
